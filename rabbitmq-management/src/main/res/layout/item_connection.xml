<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clipToPadding="false"
    android:orientation="vertical"
    android:paddingBottom="@dimen/size_lower"
    android:paddingEnd="@dimen/size_medium"
    android:paddingLeft="@dimen/size_medium"
    android:paddingRight="@dimen/size_medium"
    android:paddingStart="@dimen/size_medium"
    android:paddingTop="@dimen/size_lower"
    >

  <include
      layout="@layout/item_connection_details"
      android:id="@+id/details"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_below="@+id/basics"
      android:visibility="gone"
      />

  <android.support.v7.widget.CardView
      android:id="@id/basics"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      card:contentPadding="@dimen/size_medium"
      >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >

      <LinearLayout
          android:layout_width="match_parent"
          android:layout_height="wrap_content"
          android:layout_alignParentLeft="true"
          android:layout_alignParentStart="true"
          android:layout_toLeftOf="@+id/more"
          android:layout_toStartOf="@id/more"
          android:orientation="vertical"
          >

        <TextView
            android:id="@+id/name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/size_lowest"
            android:textStyle="bold"
            tools:text="172.17.0.1:46166 (1)"
            />

        <TextView
            android:id="@+id/user"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/size_lowest"
            tools:text="test"
            />

      </LinearLayout>

      <ImageButton
          android:id="@id/more"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_alignParentEnd="true"
          android:layout_alignParentRight="true"
          android:layout_centerVertical="true"
          android:contentDescription="@null"
          android:minWidth="@dimen/size_medium"
          android:src="@drawable/ic_expand_more_gray_24dp"
          style="@style/RabbitmqManagement.Widget.Button.Borderless.Colored"
          />

    </RelativeLayout>

  </android.support.v7.widget.CardView>

</RelativeLayout>
